🐧 Tux Assistant — Guía de Instalación (Ubuntu)

Asistente de escritorio con IA local usando **Ollama + PyQt5**.

---

## 📌 Requisitos previos

* Sistema operativo: **Ubuntu / Linux**
* Python **3.9 o superior**
* Arquitectura **x86_64**
* Conexión a internet (solo para descargar el modelo)

Verificar Python:

codigo:

    python3 --version


---

## 1️⃣ Instalar Ollama (IA local)

- Instalar Ollama:

codigo:

    curl -fsSL https://ollama.com/install.sh | sh


- Verificar instalación:

codigo:

    ollama --version


- Descargar un modelo potente (una sola vez):

codigo:

    ollama pull mistral


> ℹ️ Ollama se ejecuta como servicio local.
> No es necesario usar `ollama run` manualmente:
> **Tux Assistant se encarga de comunicarse con él automáticamente.**

---

## 2️⃣ Crear entorno virtual e instalar dependencias

- Ubícate en la carpeta del proyecto:

codigo:

    cd /home/sergio-lozano/tux-assistant


- Crear entorno virtual:

codigo:

    python3 -m venv venv


- Activarlo:

codigo:

    source venv/bin/activate


- Instalar dependencias:

codigo:

    pip install -r requirements.txt


---

## 3️⃣ Dar permisos al ejecutable

codigo:

    chmod +x /home/sergio-lozano/tux-assistant/run.sh

---

## 4️⃣ Crear acceso directo en el sistema

- Crear archivo `.desktop`:

 codigo:
    nano ~/.local/share/applications/tux-assistant.desktop


- Pegar **exactamente** lo siguiente:


[Desktop Entry]
Name=Tux Assistant
Comment=Asistente de escritorio Linux con IA local
Exec=/home/sergio-lozano/tux-assistant/run.sh
Icon=/home/sergio-lozano/tux-assistant/assets/tux.png
Terminal=false
Type=Application
Categories=Utility;AI;
StartupNotify=false


Guardar:

* `Ctrl + O`
* `Enter`
* `Ctrl + X`

Dar permisos:

codigo:

    chmod +x ~/.local/share/applications/tux-assistant.desktop

---

## 5️⃣ Ejecutar el asistente

* Abre **Actividades**
* Escribe **Tux Assistant**
* Haz clic 🐧

El asistente aparecerá flotando en el escritorio.

---

## ⭐ Opcional: iniciar automáticamente al encender el sistema

codigo:

    cp ~/.local/share/applications/tux-assistant.desktop ~/.config/autostart/

---

## 📦 Dependencias utilizadas

```requirements.txt

PyQt5==5.15.10
PyQt5-Qt5==5.15.2
PyQt5-sip==12.13.0
pygame==2.5.2
psutil==5.9.8
requests==2.31.0
urllib3==2.1.0
certifi==2024.2.2
charset-normalizer==3.3.2
idna==3.6

```

---

## 🐧 Nota final

Tux Assistant:

* Usa **IA local**
* No requiere tokens
* No tiene límites de uso
* Funciona completamente offline (tras descargar el modelo)

---